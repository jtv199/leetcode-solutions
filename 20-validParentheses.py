from collections import defaultdict

class Solution:
    P_PAIRS = '[]{}<>()'
    PAIRS_DICT = {}

    def isValid(self, s: str) -> bool:
        self.counter_dict = defaultdict(int)
    return

    def convert_pairs_to_dict(self):
        choose = lambda a: if a == 0:

        [self.PAIRS_DICT[i] = 2-(ind%2)]
        self.PAIRS_DICT =

    def add_to_counter_dict(self, c):
